{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": ["g++ cparse/builtin-features.o cparse/core-shunting-yard.o cito_dev.cpp -o cito -std=c++17 && ./cito"]
        }
    ]
}